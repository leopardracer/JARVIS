# Claude API key: https://platform.claude.com/
ANTHROPIC_API_KEY=

# Optional: override the model used by the chat (default: claude-opus-5)
JARVIS_MODEL=

# Optional: dedicated RPC endpoints (Alchemy, Chainstack, QuickNode...).
# Without them the rate-limited public Robinhood Chain RPC is used.
ROBINHOOD_RPC_URL_MAINNET=
ROBINHOOD_RPC_URL_TESTNET=
